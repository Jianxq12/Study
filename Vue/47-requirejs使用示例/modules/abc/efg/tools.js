define(function() {
    var toolBox = {
        banshou: {
            size: "8"
        },
        langtou: {
            size: "10"
        },
        dianzuan: {
            brand: "博世"
        }
    }
    //如果当前模块要将内容导出，交给别人使用
    //只需要简单的将需要导出的内容，进行return即可

    return toolBox;
});